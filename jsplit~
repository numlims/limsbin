#!/usr/bin/bash
# jsplit: split input into json array
# usage: echo "1,2,3" | jsplit ,
# todo: handle new lines in input
d=$1
# if delimiter is not new line, make new lines explicit
#if ! [[ $d == "\n" ]]; then
#   awk '{printf $0 "\\n"}'
#fi
perl -pe 's/\n//g' | tr $d '\n' | jarray